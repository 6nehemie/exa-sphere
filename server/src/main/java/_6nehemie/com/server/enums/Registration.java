package _6nehemie.com.server.enums;

/**
 * Enum for registration
 * @param CREDENTIALS - registration with credentials
 * @param OAUTH - registration with oauth
 */
public enum Registration {
    CREDENTIALS,
    OAUTH
}
